version: '3.1'

intents:
  - greet
  - ask_location
  - choose_place_type
  - set_language

entities:
  - place
  - language

slots:
  place:
    type: text
    influence_conversation: false
    mappings:
      - type: from_entity
        entity: place

  language:
    type: text
    influence_conversation: false
    mappings:
      - type: from_entity
        entity: language
        intent: set_language

  pending_number:
    type: text
    influence_conversation: false
    mappings:
      - type: custom

  place_type:
    type: text
    influence_conversation: false
    mappings:
      - type: custom

responses:
  utter_greet:
    - text: |
        Сайн уу 👋
        Би МУИС-ийн **хичээлийн байр** / **дотуур байр** / **төвүүдийн** байршлыг (Google Maps линкээр) өгдөг туслах бот.

        Та ингэж асууж болно:
        • “МУИС, хичээлийн 1-р байр (төв байр)”
        • “МУИС, хичээлийн 2-р байр” / “Нийгмийн шинжлэх ухааны сургууль”
        • “МУИС, хичээлийн 3-р байр” / “Математик компьютерийн сургууль”
        • “МУИС, хичээлийн 4-р байр”
        • “МУИС, хичээлийн 5-р байр”
        • “МУИС, хичээлийн 7-р байр”
        • “МУИС, хичээлийн 8-р байр”
        • “МУИС цахим номын сан”
        • “1-р дотуур байр”, “гадаад оюутны байр”
        • “байршлууд” (жагсаалт)

        ⚠️ Хэрвээ та зөвхөн “1”, “2”, “2-р байр”, “4-р байр” гэх мэтээр бичвэл (хичээлийн байр уу, дотуур байр уу гэдэг нь) тодорхойгүй байж болдог тул би асууж тодруулна.

  utter_ask_place_type:
    - text: |
        МУИС-ийн **хичээлийн байр** уу эсвэл **дотуур байр** уу?
        (Ж: “хичээлийн байр”, “дотуур байр” гэж хариулаарай.)

  utter_language_set:
    - text: OK ✅ Language set.

actions:
  - action_send_location

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
